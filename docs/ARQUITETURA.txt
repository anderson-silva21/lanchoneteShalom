Arquitetura do projeto SH82

Este projeto é um monorepo simples com duas partes principais:

1. frontend/
   - Aplicação React rodando com Vite.
   - Interface de PDV, dashboard, relatórios e views administrativas.
   - Comunicação com o backend via API REST em /api/*.

2. backend/
   - API Express em Node.js.
   - Persistência em SQLite (database/lanchonete.sqlite) via better-sqlite3.
   - Rotas organizadas por domínio:
     - autenticação (/api/auth)
     - usuarios administrativos (/api/users)
     - produtos, estoque e vendas, incluindo produtos recebidos por doacao sem custo
     - relatórios e exportação
     - backup, restauracao e auditoria operacional
     - planilha central
     - Livraria/Shalom Store (/api/library e /api/public/library)

O root package.json orquestra ambos:
- npm run dev inicia backend e frontend em paralelo.
- npm run build --prefix frontend gera a versão estática do frontend.
- npm start --prefix backend inicia apenas a API.

---

Escolhas de tecnologia

Frontend
- React com Vite
  - Desenvolvimento rápido, recarga instantânea e bundle leve.
- TailwindCSS
  - Estilização utilitária para layout responsivo e interface limpa.
- Recharts
  - Gráficos para dashboard de vendas e indicadores.
- react-router-dom
  - Navegação de tela única.
- lucide-react
  - Ícones simples e leves para botões e status.

Backend
- Node.js + Express
  - Servidor REST leve e flexível para pequenas aplicações.
- better-sqlite3
  - SQLite embarcado, ideal para sistemas pequenos com instalação simples e alta performance em disco local.
- dotenv
  - Configurações externas como PORT, DB_PATH, JWT_SECRET e CORS_ORIGINS.
- helmet
  - Headers de segurança HTTP.
- cors
  - Controle de origens permitidas para o frontend.
- bcryptjs + jsonwebtoken
  - Autenticação segura com hash de senhas e tokens JWT.
- zod
  - Validação de dados no backend, garantindo contratos de API mais estáveis.
- pdfkit, exceljs, json2csv
  - Geração de exportações em PDF, XLSX e CSV.

---

Padrões e separação de responsabilidades

- backend/src/routes/*
  - Cada domínio tem sua própria rota, o que facilita manutenção.
  - Rotas publicas e autenticadas da Livraria ficam separadas para evitar vazamento de campos internos.
- backend/src/middleware/*
  - Logging, tratamento de erros e controle de requisições ficam centralizados.
  - backend/src/db.js
     - Inicialização do banco e configuração de caminho do arquivo SQLite.
     - Cria schema e usuarios iniciais, mas nao popula produtos, estoque, vendas, combos ou eventos ficticios.
     - Executa migracoes leves e cria backup pre-migracao antes de alteracoes destrutivas de schema.
     - Cria entidades independentes para a Livraria: library_categories, library_products, library_product_images, library_inventory_movements, library_sales e library_sale_items.
  - frontend/src/components/*
     - UI modularizada em componentes reutilizáveis.
     - Sistema concentra saude, permissao, auditoria e configuracoes administrativas.
     - LibraryManager concentra a operacao autenticada da Livraria.
     - PublicLibraryStorefront renderiza a vitrine publica sem exigir login.

---

Dominio Livraria / Shalom Store

- A Livraria nao reutiliza as tabelas products, stock_batches, sales e sale_items da Lanchonete.
- O estoque da Livraria e mantido em library_products.stock_quantity e auditado em library_inventory_movements.
- Vendas da Livraria sao gravadas em library_sales e library_sale_items.
- A criacao de venda e transacional: se algum item nao tiver estoque suficiente, a venda e abortada e o estoque permanece inalterado.
- A coluna library_sales.idempotency_key evita duplicidade quando o operador clica duas vezes ou reenvia uma requisicao.
- O catalogo publico retorna somente campos de vitrine: nome, descricao, categoria, SKU, preco, disponibilidade, imagens e URL publica. Custo, lucro e historico nao sao expostos.
- O contato publico usa carrinhos assistidos com referencia segura; o backend atribui um vendedor da Livraria por round-robin persistido e monta o WhatsApp a partir do telefone configurado no cadastro desse vendedor.

---

Autorizacao

- O backend continua sendo a fonte de verdade para autorizacao.
- Regras de telas e permissoes ficam em backend/src/middleware/accessControl.js.
- admin: tudo.
- finance: leitura financeira/operacional nos dois dominios, sem gestao de usuarios e sem mutacoes operacionais da Livraria.
- library: operacao da Livraria, sem acesso automatico a Lanchonete.
- manager e cashier: permissoes existentes da Lanchonete preservadas.
- O frontend tambem esconde menus conforme o perfil, mas isso e apenas UX; as rotas autenticadas continuam protegidas no backend.

---

Por que essa arquitetura faz sentido aqui

- Projeto focado em uma lanchonete pequena, então:
  - SQLite é suficiente e evita overhead de banco separado.
  - Backend em Node/Express permite APIs rápidas sem infraestrutura complexa.
  - Frontend em React/Vite oferece boa experiência de usuário com pouco setup.
- Separação frontend/backend permite:
  - servir frontend estático em qualquer host
  - escalar backend independentemente
  - usar CORS para desenvolvimento local com localhost:5173 + localhost:4000

---

Pontos importantes

- É uma aplicação clássica SPA + API.
- O backend é responsável por:
  - autenticação
  - gestao de usuarios ativos, exclusao logica de acessos e troca obrigatoria de senha temporaria
  - perfis de acesso admin, manager, finance e cashier
  - configuracao administrativa para habilitar ou desabilitar a carga inicial
  - regras de venda e inventário, incluindo custo zero para produtos doados
  - alertas agendados via Telegram para estoque, validades e pagamentos pendentes
  - configuracao dos alertas Telegram via banco, com .env como fallback para credenciais sensiveis
  - auditoria de acoes criticas e historico por produto
  - registro de fechamento de caixa/evento
  - backup manual, automatico, pre-migracao e restauracao controlada
  - limpeza administrativa de dados operacionais para primeira carga real
  - exportação
- O frontend é responsável por:
  - UI de PDV
  - menu de carga inicial do inventário real quando habilitado pelo admin
  - tela administrativa para verificar e testar alertas do Telegram
  - tela administrativa para saude do sistema, matriz de permissoes e auditoria recente
  - importacao CSV na carga inicial
  - dashboard e relatórios
  - fluxo de login e experiência do usuário
  - tela de troca obrigatoria de senha no primeiro acesso ou apos reset
